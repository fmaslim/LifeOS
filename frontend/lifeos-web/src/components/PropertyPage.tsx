import type{PropertyData}from'../models/property';import'./WorkspaceCollection.css';export function PropertyPage({data}:{data:PropertyData}){const occupied=data.units.filter(u=>u.occupied).length,totalRent=data.units.filter(u=>u.occupied).reduce((n,u)=>n+u.rent,0);return <div className="dashboard"><section className="workspace-hero"><div><p className="eyebrow">Private local operations</p><h1>Property</h1><p className="subtitle">{data.name} · {occupied}/{data.units.length} occupied · ${totalRent.toLocaleString()} monthly rent</p></div></section><section className="workspace-grid">{data.units.map(u=><article className="workspace-card" key={u.id}><header><span className="workspace-pill">{u.name}</span><small>{u.payment}</small></header><h2>{u.tenant}</h2><p>${u.rent}/month</p><footer><span>Lease end {u.leaseEnd}</span><small>{u.occupied?'Occupied':'Available'}</small></footer></article>)}</section><section className="panel workspace-detail"><p className="eyebrow">Upcoming actions</p><h2>Maintenance</h2><div className="workspace-list">{data.maintenance.map(m=><article key={m.id}><span>{m.title}</span><small>{m.due} · {m.status}</small></article>)}</div></section></div>}
