name: Backend validation

on:
  pull_request:
    paths: ['backend/**', '.github/workflows/backend.yml']
  push:
    branches: [main]
    paths: ['backend/**', '.github/workflows/backend.yml']

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - run: dotnet restore backend/LifeOS.Persistence.Tests/LifeOS.Persistence.Tests.csproj
      - run: dotnet build backend/LifeOS.Persistence.Tests/LifeOS.Persistence.Tests.csproj --configuration Release --no-restore
      - run: dotnet run --project backend/LifeOS.Persistence.Tests/LifeOS.Persistence.Tests.csproj --configuration Release --no-build
